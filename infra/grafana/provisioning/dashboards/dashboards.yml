apiVersion: 1

providers:
  - name: "ai-vpn-dashboards"
    orgId: 1
    folder: "AI VPN Platform"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
